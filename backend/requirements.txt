fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.2
asyncpg==0.29.0
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
httpx==0.27.2
feedparser==6.0.11
pandas==2.2.3
numpy==2.0.2
pandas-ta-classic==0.6.52
torch==2.4.1
transformers==4.45.1
huggingface-hub==0.25.1
PyPortfolioOpt==1.6.0
openai==1.51.0
yfinance==0.2.44
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4's bcrypt backend probe crashes on bcrypt>=4.1 (removed __about__)
python-jose[cryptography]==3.3.0
python-multipart==0.0.9
tenacity==9.0.0
PyYAML==6.0.2

# Kronos (github.com/shiyu-coder/Kronos) itself is NOT pip-installable -- no
# setup.py/pyproject.toml. It's cloned separately (see backend/Dockerfile and
# run.sh) and put on sys.path by app/models_iface/kronos.py. These are just
# its own runtime dependencies not already covered above (its own
# requirements.txt also pins matplotlib/tqdm, which are only needed for its
# plotting/demo scripts, not headless inference -- skipped here).
einops==0.8.1
safetensors==0.6.2

# FinRL DRL portfolio agent -- train-once-offline path (Section 12), see
# app/models_iface/portfolio_finrl.py and scripts/train_finrl_agent.py.
# Not required for the app to run; only needed if training/using that agent.
stable-baselines3==2.5.0
gymnasium==0.29.1

pytest==8.3.3
pytest-asyncio==0.24.0
respx==0.21.1
